import { Link } from "react-router";


const Cancel = () => {
    return (
        <div>
            <h1>The payment is Cancel</h1>
            <Link to='/'>Go Home</Link>
        </div>
    );
};

export default Cancel;